name: PR Checks

on:
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  checks:
    uses: defi-wonderland/aztec-ci-actions/.github/workflows/run-tests.yml@v0
    secrets: inherit

  benchmark:
    uses: defi-wonderland/aztec-benchmark/.github/workflows/pr-benchmark.yml@5022f356e113c73ff42822f49186c56ee34b697d
    permissions:
      pull-requests: write
      issues: write
      actions: read
    secrets: inherit
